#include <main.h>
#include <string>

using std::cout;
using std::cin;
using std::getline;
using std::string;
using std::vector;
using std::string;

int main() {

    std::cout << std::srand(time(NULL)) << "\n";

    return 0;
}
